{
  "author": "David Mikalova",
  "dependencies": {
    "bootstrap": "^4.4.1",
    "daemonite-material": "^4.1.1",
    "jquery": "^3.4.1",
    "popper.js": "^1.16.1"
  },
  "description": "Make quick and simple lists.",
  "devDependencies": {
    "copyfiles": "^2.0.0",
    "imagemin-cli": "^3.0.0",
    "lite-server": "^2.5.4",
    "node-sass": "^4.13.1",
    "onchange": "^6.1.0",
    "parallelshell": "^3.0.1",
    "rimraf": "^2.6.2",
    "usemin-cli": "^0.6.0"
  },
  "license": "ISC",
  "main": "index.js",
  "name": "lists",
  "repository": "https://gitlab.com/dmikalova/lists",
  "scripts": {
    "build": "npm run clean && npm run imagemin && npm run usemin",
    "clean": "rimraf dist",
    "imagemin": "imagemin assets/* -o dist/assets",
    "lite": "lite-server",
    "scss": "node-sass scss/ -o css/",
    "start": "npm run watch:all",
    "test": "echo \"Error: no test specified\" && exit 1",
    "usemin": "usemin about.html -d dist --htmlmin -o dist/about.html && usemin index.html -d dist --htmlmin -o dist/index.html && usemin list-groceries.html -d dist --htmlmin -o dist/list-groceries.html",
    "watch:all": "parallelshell 'npm run watch:scss' 'npm run lite'",
    "watch:scss": "onchange 'scss/' -- npm run scss"
  },
  "version": "0.1.0"
}
